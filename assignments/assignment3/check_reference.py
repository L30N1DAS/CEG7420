#Hint: to check the type of a reference, you can follow the example like this. Here i is a reference object, and we want to check whether this reference is an "UNCONDITIONAL_JUMP" reference.

i.getReferenceType() == RefType.UNCONDITIONAL_JUMP: